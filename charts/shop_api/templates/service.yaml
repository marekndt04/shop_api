apiVersion: v1
kind: Service
metadata:
  name: {{ include "shop_api.fullname" . }}
  labels:
    {{- include "shop_api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shop_api.selectorLabels" . | nindent 4 }}
