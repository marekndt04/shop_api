[tool.mypy]
exclude = ["tests", ".tests"]
strict = true

[[tool.mypy.overrides]]
module = "tests.*"
ignore_errors = true

[tool.black]
line-length = 100

[tool.isort]
profile = "black"

[tool.coverage.run]
source = ["src"]
omit = ["tests/**", "**/__init__.py", "src/config.py"]